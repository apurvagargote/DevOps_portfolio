services:
  frontend:
    build:
      context: ./frontend
      dockerfile: infrastructure/Dockerfile
    image: apurvagargote/portfolio-frontend:latest
    ports:
      - "2000:80"
    restart: always
    depends_on:
      - backend
    networks:
      - portfolio-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: apurvagargote/portfolio-backend:latest
    ports:
      - "5000:5000"
    restart: always
    environment:
      - MONGODB_URI=mongodb+srv://apurvagargote:apurva10@cluster0.c6ntv.mongodb.net/portfolio?retryWrites=true&w=majority
      - PORT=5000
      - EMAIL_USER=apurvagargote@gmail.com
      - EMAIL_PASS=nqcn waqp bzgz hmpu
      - EMAIL_TO=apurvagargote@gmail.com
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
